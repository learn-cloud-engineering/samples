# hello-nginx-ec2

This project sets up an EC2 instance that runs a "Hello World" nginx web server inside a Docker container.

Run the `terraform output` command in your terminal to view the public IP address of the instance and visit it via `curl` or a web browser.

![screenshot](/hello-nginx-ec2/images/image.png)
