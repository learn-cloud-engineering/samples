# hello-world-ec2

This project sets up an EC2 instance that runs a "Hello World" web server.

Run this command to get the public IP address of the instance. You can use `curl` or visit the site from your browser.

```sh
terraform output
```

![screenshot](/hello-world-ec2/images/image.png)
